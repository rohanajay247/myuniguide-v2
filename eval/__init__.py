"""Evaluation harness.

This package may read the corpus ground truth. `src/myuniguide` may not.
That boundary is enforced by tests/test_corpus_isolation.py and is the whole
reason the two live in separate packages.
"""
